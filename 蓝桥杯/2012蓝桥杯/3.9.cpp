#include <stdio.h>
#include <algorithm>
using namespace std;

int main()
{
	int n;
	int a[9]={0};
	for(int i=0;i<9;i++)
		a[i] = i+1;
	scanf("%d",&n);
	do
	{
		for(int i=0;i<n;i++)
		{
			if(i==)
			
			n-i
		}
	}while(next_permutation(a,a+9));
		
	return 0;
}
